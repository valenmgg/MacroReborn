# Detecta a quien choca repetidamente contra el limite de peticiones.
# Un visitante normal nunca ve un 429; quien acumula varios esta forzando.
[Definition]
failregex = ^<HOST> .* "(GET|POST|HEAD|PUT|DELETE).*" 429
ignoreregex =
